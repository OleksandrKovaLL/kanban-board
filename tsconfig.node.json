{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "CommonJS"
    // Add Node.js-specific configurations
  },
  "include": [
    "server/**/*"
  ],
  // Include server files
  "exclude": [
    "node_modules",
    "src/**/*"
  ]
  // Exclude app files
}